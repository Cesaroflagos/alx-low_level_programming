#include <stdio.h>
/**
 * main - Starting point
 * Return: Repeat 0 (Done)
 */

int main(void)
{
	char cr;

	cr = 'a';
	while
		(cr <= 'z') {
			putchar(cr);
			cr++;
		}
	putchar('\n');
	return (0);
}
