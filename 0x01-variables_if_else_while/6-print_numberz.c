#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Starting point
 * Return: Repeat 0 (Done)
 */
int main(void)
{
	int a;

	for (a = 0; a < 9; a++)
	{
	putchar(a);
	}
	putchar('\n');

	return (0);
}
