#include <stdio.h>
/**
 * main - Starting point
 * Return: Repeat 0 (Done)
 */

int main(void)
{
	char cr;

	char hr;

	cr = 'a';
	hr = 'A';
	while
		(cr <= 'z') {
			putchar(cr);
			cr++;
		}
	while
		(hr <= 'Z') {
			putchar(hr);
			hr++;
		}
	putchar('\n');
	return (0);
}
